# ===== 密钥（只从环境变量读，无 CLI 入口，勿提交）=====
LLM_API_KEY=sk-xxx
WIGOLO_TOKEN=

# ===== 非敏感配置：默认值内联在代码里，用 CLI 参数覆盖（--db / --llm-base-url / --llm-model / --embedding-model）=====

# ===== 网络代理（opencode.ai / HuggingFace 下载模型需要）=====
HTTP_PROXY=http://127.0.0.1:7890
HTTPS_PROXY=http://127.0.0.1:7890
